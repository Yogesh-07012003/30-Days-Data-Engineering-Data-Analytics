# Day 07 — SQL Aggregations

## 🎯 Objective

Learn and practice SQL aggregate functions and grouping concepts used in data analysis and data engineering.

## 📚 Topics Covered

- `COUNT()`
- `SUM()`
- `AVG()`
- `MIN()`
- `MAX()`
- `GROUP BY`
- `HAVING`
- `WHERE` with aggregations
- `GROUP BY` with multiple columns
- `ORDER BY` with aggregations
- `LIMIT` with aggregations
- `COUNT(DISTINCT ...)`
- `ROUND()`
- Revenue calculations
- `WHERE` vs `HAVING`
- SQL query execution order

---

## 🗂️ Files

```text
Day07/
├── sales_aggregation.sql
└── README.md
